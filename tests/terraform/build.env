PROJECT=hashicorp/terraform
DOWNLOAD_TEMPLATE=https://releases.hashicorp.com/terraform/$(echo "${VERSION}" | cut -c2-)/${NAME}_$(echo "${VERSION}" | cut -c2-)_${GOOS}_${GOARCH}.zip
VERIFY_COMMAND=terraform version
